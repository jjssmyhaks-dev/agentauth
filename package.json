{
  "name": "agentauth",
  "version": "1.0.0",
  "description": "AgentAuth - Identity & Permissions for AI Agents",
  "private": true,
  "scripts": {
    "preview": "concurrently --names backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev": "npm run preview",
    "dev:backend": "cd backend && PORT=4000 npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "concurrently --names backend,frontend -c blue,magenta \"npm:start:backend\" \"npm:start:frontend\"",
    "start:backend": "cd backend && PORT=4000 npm run start:prod",
    "start:frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
