# ── Root dashboard (Vite) ──────────────────────────────────────────────
# Port for the dev server (default 5173)
PORT=5173

# Point the dashboard at a running AgentAuth backend to use real data
# instead of the in-memory demo dataset. Leave unset for demo mode.
# VITE_API_URL=http://localhost:4000
